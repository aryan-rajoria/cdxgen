Minimal deps.edn fixture for the build-introspection fixture matrix: clojure
pins exact versions with no resolver cdxgen can drive, so the ecosystem grades
manifest (at-ceiling) whatever the environment looks like. Asserted by
lib/stages/postgen/introspection/e2e.poku.js (Group C1) and
ci/introspection-tests.sh.
