{
  "description": "Zig build.zig.zon parse coverage. Manifest-backed: ZON dependencies, no toolchain.",
  "fixture": "../../data/zig-smoke",
  "scenarios": [
    {
      "name": "default",
      "projectType": ["zig"],
      "options": {}
    }
  ]
}
