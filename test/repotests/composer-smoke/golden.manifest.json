{
  "description": "PHP composer.lock parse coverage. Lockfile-backed: pinned package versions.",
  "scenarios": [
    {
      "name": "default",
      "projectType": ["composer"],
      "options": {}
    }
  ]
}
