Polyglot fixture for the build-introspection Group D row (per-ecosystem
isolation): go.mod, package.json + package-lock.json, pyproject.toml +
poetry.lock and pom.xml in one tree. Degrading only the Maven toolchain
(MVN_CMD) must move only the java tier. Asserted by
lib/stages/postgen/introspection/loop.poku.js.
