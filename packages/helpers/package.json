{
  "name": "@cdxgen/internal-helpers",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal workspace package: lib/helpers/** \u2014 shared utilities, parsers, and ecosystem helpers. Catch-all pending D03 (utils.js decomposition). Files remain at lib/helpers/ on disk; the published tarball layout is unchanged.",
  "libPath": "../../lib/helpers",
  "scripts": {
    "test": "pnpm -w exec poku lib/helpers/"
  },
  "layer": 2
}
