{
  "name": "@cdxgen/internal-inventory",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal workspace package: lib/inventory/** — general-purpose BOM construction utilities shared across all ecosystems (purl, SPDX, evidence, deps, display, analyzers, AI/MCP/HBOM, formulation parsers, CI parsers, crypto, OS/osquery, source resolution, and related tooling). No imports from ecosystem-specific or orchestration layers.",
  "libPath": "../../lib/inventory",
  "scripts": {
    "test": "pnpm -w exec poku lib/inventory/"
  },
  "layer": 2
}
