{
  "name": "@cdxgen/internal-cli",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Internal workspace package: lib/cli/** \u2014 core BOM generation orchestration. Top of the dependency stack; may import from all lower layers.",
  "libPath": "../../lib/cli",
  "scripts": {
    "test": "pnpm -w exec poku lib/cli/"
  },
  "layer": 5
}
